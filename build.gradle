apply plugin: 'groovy'

configurations {
	build
}

repositories {
    mavenCentral()
}

dependencies {
    groovy group: 'org.codehaus.groovy', name: 'groovy', version: '1.7.+'
    compile group: 'commons-codec', name: 'commons-codec', version: '1.3'
    testCompile group: 'junit', name: 'junit', version: '4.+'
    testCompile group: 'org.gmock', name: 'gmock', version: '0.8.+'
}

apply from: "file:${rootDir}/clover.gradle"
